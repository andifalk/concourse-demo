---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: frolvlad/alpine-oraclejdk8
    tag: slim

inputs:
- name: demo-app

outputs:
- name: target-build
  path: build/libs

run:
  path: ./gradlew
  dir: demo-app
  args:
  - clean
  - build